import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sid.app',
  appName: 'SID',
  webDir: 'dist/Angular10FirebaseCrudDB',
  bundledWebRuntime: false
};

export default config;
